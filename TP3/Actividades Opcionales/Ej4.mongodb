use ('tiendaOnline');

db.productos.aggregate([
    {
        $unwind: "$valoraciones",
    },
    {
        $group: {
            _id: "$valoraciones.puntuacion",
            cantidadValoracion: { $sum: 1 },
        }
    },
])